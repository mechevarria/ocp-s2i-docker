#!/bin/sh

/usr/local/s2i/assemble

echo "****** Copying over reloadable context to conf directory ******"
cp -v /tmp/src/ROOT.xml ${JWS_HOME}conf/Catalina/localhost

custom=jpda-launch.sh

echo "****** Copying over ${custom} to bin directory ******"

cp -v /tmp/src/${custom} ${JWS_HOME}/bin
